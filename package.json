{
  "name": "heroku-deploy",
  "version": "1.0.0",
  "description": "Simple deployment of a node.js app on heroku",
  "main": "server.js",
  "engines": {
    "node": "9.4.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Pierre Grardel",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "hbs": "^4.0.1"
  },
  "devDependencies": {
    "dotenv": "^5.0.0"
  }
}
